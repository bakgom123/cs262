import React from 'react';
import { Text } from 'react-native';

export default function AboutScreen({ navigation }) {
    return (
        <Text>This application acesses information on Monopoly players.</Text>
    );
};